# Direct runtime dependencies
# Compiled to requirements.txt via: pip-compile requirements.in --output-file requirements.txt --strip-extras
# pip-tools is installed in the dev container and is NOT listed here — it is a build tool only.

# Web framework
fastapi>=0.115.9
uvicorn[standard]>=0.46.0

# Data validation
pydantic>=2.13.3
pydantic-settings>=2.9.1

# Vector DB
chromadb>=1.0.7

# LLM / embeddings
ollama>=0.4.8

# PII masking
presidio-analyzer>=2.2.358
presidio-anonymizer>=2.2.358
spacy>=3.8.14

# Metrics
prometheus-fastapi-instrumentator>=7.1.0
prometheus-client>=0.21.1

# PDF ingestion
pymupdf>=1.25.5

# Database / ORM
sqlalchemy>=2.0.41
aiosqlite>=0.21.0
greenlet>=3.5.0  # Required for SQLAlchemy async operations

# Testing & dev tooling (also installed in prod image for CI — kept in one file for simplicity)
pytest>=8.3.5
pytest-asyncio>=0.26.0
httpx>=0.28.1
ruff>=0.11.9
